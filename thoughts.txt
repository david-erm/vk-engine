# WORK: 
- integrate bindgen to build system. extensions decided at build time.
    - maybe i need to allow pfn to be null? if i simply remove the extension funtions stuff will not compile.
- generate slang structs from zig structs
- maybe create system to create image view from imageHandle 
- make POSE lib for shaders
- export a vk format to avoid stuff having to import vk proper

# CHALLENGE:
- fix the text rendering, make it actually good (Vertex pulling, only 1 quad for char? or 1 unit quad and 2 scale floats?)
- add a post process pass # now do it with a offscreen image
    - really just do a deferred renderer
- do custom vulkan allocator
- upload queue for asset thingy # ?
- COULD make VkImages reference counted to VkImageViews so explicit tracking is uneeded
- mipmap gen in baker (prolly will need a vk instance (could enable doing cooler shit aswell ?)
- mikktspace
- drop ktx-software
    - just use zstd straight up
    - writing ktx2 not very hard?
- meshoptimizer
- TrueType instead of freetype

# GOAL:
- start making abstractions
- lets do aimtrainer (hopefully builds into somee wildwest type shi)

# IDEA:
- separate type definitions into its own module so that generated files can consume them?
- doing collisions via simpler meshes 
- freetype harfbuzz for real font renderind at some point // maybe can cope with only freetype? 
- pub const <alias> = .val for enums
- move to non c ktx parser thingy
- intrusive free list downsides?
- 1 canonical view of an image



