struct Render {
	opaque:
	opaque_pipeline:
	translucent:
	translucent_pipeline:
	
}


add a post process pass

finish allocator

vertex pulling over
	surely can check for performance differences

lets do aimtrainer (hopefully builds into somee wildwest type shi)

#####

integrate bindgen to build system. extensions decided at build time.
